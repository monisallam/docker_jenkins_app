version: "2"

services:
  voting-app:
    image: 172.28.128.4/admin/frontend-userinput:latest
    ports:
      - "5002:80"
    networks:
      - back-tier

  result-app:
    #build: ./result-app/.
    image: 172.28.128.4/admin/frontend-display:latest
    ports:
      - "5003:80"
    networks:
      - back-tier

  worker:
    #build: ./worker
    image: 172.28.128.4/admin/myworker
    networks:
      - back-tier

  redis:
    image: redis:alpine
    ports: ["6379"]
    networks:
      - back-tier

  db:
    image: 172.28.128.4/admin/mydb:9.4
    volumes:
      - "db-data:/var/lib/postgresql/data"
    networks:
      - back-tier

volumes:
  db-data:

networks:
  back-tier:
    ipam:
        driver: default
        config:
        - subnet: 10.0.201.0/24
          gateway: 10.0.201.1
